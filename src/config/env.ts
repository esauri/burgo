/**
 * @NOTE ⚠️ DO NOT IMPORT!
 * To access config, use config (for server + client rendering) or configServer (server only)
 */
export const env = {
  SUPABASE_ANON_KEY: process.env.SUPABASE_ANON_KEY,
  SUPABASE_URL: process.env.SUPABASE_URL,
};
